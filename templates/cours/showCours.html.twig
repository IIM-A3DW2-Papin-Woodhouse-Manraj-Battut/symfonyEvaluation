{% extends 'base.html.twig'%}

{% block body %}
    <h1>{{ cours.nom }}</h1>
    <p>{{ cours.categorie.nom }} - {{ cours.date|date('d/m/Y') }}</p>
    <p>{{ cours.description }}</p>
    <p>{{ cours.contenu }}</p>
{% endblock %}
