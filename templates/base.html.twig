<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        {% block stylesheets %}{% endblock %}
        <link href="https://unpkg.com/tailwindcss@^1.0/dist/tailwind.min.css" rel="stylesheet">
        <script src="https://unpkg.com/ionicons@5.2.3/dist/ionicons.js"></script>
    </head>
    <body class="overflow-x-hidden">
        <nav class="flex items-center justify-between flex-wrap bg-orange-400 p-6">
            <ul class="flex">
                <li class="mr-6"><a href="{{ path('categorie') }}" class="block mt-4 lg:inline-block lg:mt-0 text-white hover:text-white mr-4">Catégorie</a> </li>
                <li class="mr-6"><a href="{{ path('cours') }}" class="block mt-4 lg:inline-block lg:mt-0 text-white hover:text-white mr-4">Cours</a> </li>
                <li class="mr-6"><a href="{{ path('admin') }}" class="block mt-4 lg:inline-block lg:mt-0 text-white hover:text-white mr-4">Admin</a> </li>
                <li class="mr-6"><a href="login" class="block mt-4 lg:inline-block lg:mt-0 text-white hover:text-white mr-4">Se connecter</a> </li>
            </ul>
        </nav>
        {% for type, messages in app.flashes %}
            {% for message  in messages %}
                <p>{{type}}: {{message}}</p>
            {% endfor %}
        {% endfor %}
        {% block body %}{% endblock %}
        {% block javascripts %}{% endblock %}
    </body>
</html>
